# Photo Sync Server

Сервер синхронизации фотографий счетчиков с Android приложения MeterSync на ПК.

## Описание

Photo Sync Server - это легковесный HTTP сервер на Go, который принимает фотографии счетчиков от Android приложения MeterSync и сохраняет их на локальный компьютер с индексацией по номерам счетчиков.

## Установка

1. Скачайте файл `photo-sync-server.exe` из [релизов](https://github.com/your-username/photo-sync-server/releases)
2. Поместите файл в любую папку (рекомендуется: `C:\Program Files\PhotoSyncServer\` или рядом с другими программами)
3. Запустите файл двойным кликом
4. Сервер автоматически запустится на порту 8080

**Важно:** Сервер автоматически выберет папку для сохранения фото с учетом прав доступа:
- Сначала пробует создать папку `meter` рядом с exe файлом
- Если не получается, использует временную директорию Windows
- Если и это не работает, использует папку `Documents\photo-sync\meter`

## Использование

### Запуск сервера

1. Запустите `photo-sync-server.exe` двойным кликом
2. В окне консоли вы увидите:
   - Локальный IP адрес сервера (например: `http://192.168.1.100:8080`)
   - Путь к папке, куда будут сохраняться фото
   - Ссылку для начала синхронизации

### Запуск синхронизации

1. Откройте браузер и перейдите на сайт **meter.printecs.com**
2. В настройках скриптов найдите раздел **"Автозагрузка фото"**
3. Нажмите кнопку **"📱 Синхронизация фото"**
4. Появится модальное окно с QR-кодом и токеном

### Подключение Android приложения

**Вариант 1: Сканирование QR-кода (рекомендуется)**
1. Откройте приложение **MeterSync** на Android
2. Перейдите в раздел **"Синхронизация фото"**
3. Нажмите кнопку **"Сканировать QR-код"**
4. Наведите камеру на QR-код в браузере
5. Дождитесь завершения синхронизации

**Вариант 2: Ручной ввод токена**
1. Скопируйте токен из модального окна в браузере
2. В приложении MeterSync выберите **"Ввести токен вручную"**
3. Вставьте токен
4. Введите IP адрес сервера (показан в консоли сервера)
5. Введите порт: `8080`
6. Нажмите **"Начать синхронизацию"**

### Ожидание синхронизации

1. После подключения Android приложение начнет загружать фото
2. В браузере вы увидите прогресс синхронизации (например: `5/13 фото загружено`)
3. После завершения появится сообщение **"Синхронизация завершена"**
4. Закройте модальное окно

## Где сохраняются фотографии

Фото сохраняются в папку `meter` в одном из следующих мест (в зависимости от прав доступа):

1. **Рядом с exe файлом** (предпочтительно):
   ```
   C:\Program Files\PhotoSyncServer\meter\
   ```

2. **Временная директория Windows** (если нет прав в Program Files):
   ```
   C:\Users\<ваше_имя>\AppData\Local\Temp\photo-sync\meter\
   ```

3. **Папка Documents** (если временная директория недоступна):
   ```
   C:\Users\<ваше_имя>\Documents\photo-sync\meter\
   ```

### Структура папки meter:

```
meter/
├── meter_1765447831612_1765448252746360100.jpg
├── meter_1765447837052_1765448252849028000.jpg
├── meter_1765447844702_1765448252905131700.jpg
└── .index/
    └── photo_index.json
```

**Индекс фото** (`photo_index.json`) содержит информацию о всех загруженных фото, сгруппированных по номерам счетчиков. Каждое фото имеет:
- Путь к файлу
- Дату создания
- Размер
- Хеш (для проверки дубликатов)
- USER_COMMENT из EXIF метаданных (номер счетчика)

## Требования

- **Windows 7/8/10/11** (64-bit)
- Компьютер и телефон в **одной WiFi сети**
- Никаких дополнительных установок не требуется (все библиотеки включены в exe)

## API Endpoints

- `GET /start` - Создание сессии синхронизации (возвращает токен и QR-код)
- `POST /init?token={token}` - Инициализация синхронизации (указывает количество фото)
- `POST /sync?token={token}` - Загрузка одного фото
- `GET /status?token={token}` - Статус синхронизации (прогресс)
- `GET /index?counterNumber={number}` - Получение индекса фото для указанного счетчика
- `DELETE /session?token={token}` - Удаление сессии

## Остановка сервера

Закройте окно консоли или нажмите `Ctrl+C` в консоли.

## Решение проблем

### Ошибка создания папки

Если сервер не может создать папку для фото:
1. Запустите `photo-sync-server.exe` **от имени администратора** (правый клик → "Запуск от имени администратора")
2. Или переместите exe файл в папку, где у вас есть права на запись (например, `C:\Users\<ваше_имя>\Desktop\`)

### Фото не загружаются

1. Убедитесь, что компьютер и телефон в **одной WiFi сети**
2. Проверьте, что **брандмауэр Windows** не блокирует порт 8080
3. Убедитесь, что на Android устройстве включена **синхронизация фото** в настройках приложения MeterSync

### Сервер не запускается

1. Проверьте, не занят ли порт 8080 другим приложением
2. Запустите сервер от имени администратора
3. Проверьте логи в консоли на наличие ошибок

## Лицензия

MIT

## Автор

MeterSync Team
