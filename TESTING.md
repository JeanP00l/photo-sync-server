# Инструкция по тестированию системы синхронизации фото

## Шаг 1: Запуск Go сервера

### Вариант А: Запуск через командную строку (рекомендуется для первого запуска)

1. Откройте командную строку Windows (cmd.exe)
2. Перейдите в папку с сервером:
   ```
   cd C:\Users\Admin\Desktop\photoSync\photo-sync-server
   ```
3. Запустите сервер:
   ```
   photo-sync-server.exe
   ```

**Ожидаемый результат:**
- Окно консоли остается открытым
- Появляются сообщения:
  ```
  Base directory: C:\Users\Admin\Documents\meter
  Index directory: C:\Users\Admin\Documents\meter\.index (или альтернативное расположение)
  Photo sync server starting on http://192.168.1.XXX:8080
  Photos will be saved to: C:\Users\Admin\Documents\meter
  To start sync, visit: http://localhost:8080/start
  ```

**Если окно закрывается сразу:**
- Запустите через cmd.exe (не двойным кликом)
- Скопируйте сообщение об ошибке

### Вариант Б: Запуск двойным кликом

1. Найдите файл `photo-sync-server.exe` в папке `photo-sync-server`
2. Запустите двойным кликом
3. Окно консоли должно остаться открытым

---

## Шаг 2: Проверка работы сервера

### 2.1 Проверка через браузер

1. Откройте браузер (Chrome, Edge и т.д.)
2. Перейдите по адресу: `http://localhost:8080/start`
3. **Ожидаемый результат:** JSON ответ:
   ```json
   {
     "token": "a1b2c3d4-e5f6-...",
     "url": "http://192.168.1.XXX:8080/sync?token=...",
     "localIP": "192.168.1.XXX",
     "port": 8080
   }
   ```

### 2.2 Проверка через командную строку (curl)

Откройте новую командную строку и выполните:

```cmd
curl http://localhost:8080/start
```

**Ожидаемый результат:** JSON с токеном и URL

### 2.3 Проверка статуса (без сессии)

```cmd
curl http://localhost:8080/status?token=test123
```

**Ожидаемый результат:** `{"error":"session not found"}`

---

## Шаг 3: Тестирование браузерного скрипта

### 3.1 Подготовка

1. Убедитесь, что сервер запущен (Шаг 1)
2. Откройте браузер с установленным менеджером пользовательских скриптов (Tampermonkey/Violentmonkey)
3. Убедитесь, что установлены скрипты:
   - `Менеджер общей панели настроек.txt`
   - `Автозагрузка фото в акты.txt`

### 3.2 Тестирование кнопки синхронизации

1. Откройте сайт: `https://meter.printecs.com`
2. Войдите в систему
3. Найдите панель "Настройки скриптов" (справа на экране)
4. Найдите секцию "Автозагрузка фото"
5. Нажмите кнопку **"📱 Синхронизация фото"**

**Ожидаемый результат:**
- Появляется модальное окно
- В окне отображается QR-код
- Показан токен для ручного ввода
- Показан IP адрес сервера
- Отображается статус "Ожидание подключения..."

### 3.3 Проверка генерации QR-кода

1. Убедитесь, что QR-код отображается в модальном окне
2. QR-код должен содержать JSON с данными:
   ```json
   {
     "url": "http://192.168.1.XXX:8080/sync?token=...",
     "token": "...",
     "localIP": "192.168.1.XXX",
     "port": 8080
   }
   ```

### 3.4 Проверка опроса статуса

1. После нажатия кнопки синхронизации подождите 2-3 секунды
2. В модальном окне должен появиться раздел "Прогресс"
3. Статус должен быть "Ожидание подключения..."
4. Прогресс должен показывать "0 / 0"

**Примечание:** Статус будет обновляться автоматически каждую секунду после подключения Android устройства.

---

## Шаг 4: Тестирование Android приложения

### 4.1 Подготовка

1. Убедитесь, что:
   - Сервер запущен на ПК
   - ПК и телефон в одной WiFi сети
   - На телефоне установлено приложение MeterSync

### 4.2 Подключение через QR-код

1. На ПК: Откройте модальное окно синхронизации в браузере (см. Шаг 3.2)
2. На телефоне: Откройте приложение MeterSync
3. На экране входа нажмите кнопку **"📱 Синхронизировать фото"**
4. Выберите "Сканировать QR-код"
5. Наведите камеру на QR-код в браузере

**Ожидаемый результат:**
- Приложение распознает QR-код
- Извлекает данные подключения
- Начинает поиск фото с EXIF метаданными

### 4.3 Альтернатива: Ручной ввод токена

Если камера не работает:

1. На телефоне: Выберите "Ввести токен вручную"
2. Введите токен из модального окна браузера
3. Введите IP адрес (если требуется)
4. Нажмите "Подключиться"

### 4.4 Процесс синхронизации

После подключения:

1. **На телефоне:**
   - Приложение сканирует галерею
   - Находит фото с EXIF метаданными USER_COMMENT
   - Показывает количество найденных фото
   - Начинает отправку фото на сервер

2. **В браузере:**
   - Статус меняется на "Синхронизация..."
   - Отображается прогресс (X / Y)
   - Показывается текущий файл
   - Обновляется прогресс-бар

3. **На сервере (консоль):**
   - Появляются логи о получении фото
   - Показывается информация о сохранении файлов

### 4.5 Завершение синхронизации

**Ожидаемый результат:**

1. **На телефоне:**
   - Появляется сообщение "Синхронизация завершена"
   - Показывается количество загруженных фото
   - Показывается количество пропущенных дубликатов

2. **В браузере:**
   - Статус меняется на "Завершено"
   - Прогресс показывает 100%
   - Появляется toast-уведомление с результатами

3. **На сервере:**
   - Фото сохранены в `C:\Users\Admin\Documents\meter\2025-XX\`
   - Создан/обновлен индекс в `.index\photo_index.json`

---

## Шаг 5: Проверка сохраненных файлов

### 5.1 Проверка структуры папок

1. Откройте проводник Windows
2. Перейдите в `C:\Users\Admin\Documents\meter\`
3. Проверьте наличие папок по датам (например, `2025-12\`)

**Ожидаемый результат:**
- Папки созданы по формату `YYYY-MM`
- Внутри папок находятся фото с именами: `{номер_счетчика}_{дата}_{время}.jpg`

### 5.2 Проверка индекса

1. Откройте файл `C:\Users\Admin\Documents\meter\.index\photo_index.json`
2. Проверьте структуру JSON

**Ожидаемый формат:**
```json
{
  "0011067128144508": [
    {
      "path": "2025-12/0011067128144508_20251211_120530.jpg",
      "fullPath": "C:\\Users\\Admin\\Documents\\meter\\2025-12\\...",
      "date": "2025-12-11T12:05:30Z",
      "size": 1234567,
      "hash": "a1b2c3d4..."
    }
  ]
}
```

### 5.3 Проверка через API

Откройте в браузере:
```
http://localhost:8080/index?counterNumber=0011067128144508
```

**Ожидаемый результат:** JSON со списком фото для указанного номера счетчика

---

## Шаг 6: Тестирование проверки дубликатов

### 6.1 Повторная синхронизация

1. Повторите синхронизацию с теми же фото (Шаг 4)
2. **Ожидаемый результат:**
   - Все фото должны быть пропущены как дубликаты
   - В статусе показывается `"skipped": X`
   - Файлы не дублируются на диске

### 6.2 Проверка через API статуса

После повторной синхронизации проверьте статус:
```cmd
curl http://localhost:8080/status?token={ваш_токен}
```

**Ожидаемый результат:**
```json
{
  "status": "completed",
  "total": 10,
  "uploaded": 0,
  "skipped": 10,
  "progress": 100.0
}
```

---

## Шаг 7: Тестирование интеграции с автозагрузкой фото

### 7.1 Подготовка

1. Убедитесь, что фото синхронизированы (Шаги 1-5)
2. В настройках скрипта "Автозагрузка фото" выберите директорию:
   `C:\Users\Admin\Documents\meter`

### 7.2 Тестирование автозагрузки

1. Откройте акт проверки счетчика на сайте
2. Скрипт должен автоматически найти фото по номеру счетчика
3. Фото должны автоматически загрузиться в акт

**Ожидаемый результат:**
- Фото найдены по номеру счетчика из EXIF
- Фото автоматически загружены в акт проверки
- Появляется toast-уведомление об успешной загрузке

---

## Возможные проблемы и решения

### Проблема: Сервер не запускается

**Решение:**
1. Запустите через командную строку (не двойным кликом)
2. Проверьте, не занят ли порт 8080:
   ```cmd
   netstat -an | findstr 8080
   ```
3. Если порт занят, измените `DefaultPort` в `main.go` и перекомпилируйте

### Проблема: Браузер не может подключиться к серверу

**Решение:**
1. Убедитесь, что сервер запущен
2. Проверьте адрес в браузере: `http://localhost:8080/start`
3. Проверьте настройки брандмауэра Windows
4. Убедитесь, что используется `localhost`, а не IP адрес

### Проблема: Android не может подключиться

**Решение:**
1. Убедитесь, что ПК и телефон в одной WiFi сети
2. Проверьте IP адрес в модальном окне браузера
3. Убедитесь, что используется правильный IP (не localhost)
4. Проверьте настройки брандмауэра на ПК

### Проблема: QR-код не генерируется

**Решение:**
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что библиотека QRCode загружается
3. Попробуйте использовать ручной ввод токена

### Проблема: Фото не находятся при синхронизации

**Решение:**
1. Убедитесь, что фото имеют EXIF метаданные USER_COMMENT
2. Проверьте, что фото были сделаны через приложение MeterSync
3. Проверьте разрешения приложения на доступ к галерее

---

## Дополнительные команды для тестирования

### Проверка всех endpoints через curl

```cmd
# Создание сессии
curl http://localhost:8080/start

# Инициализация (замените TOKEN на реальный токен)
curl -X POST http://localhost:8080/init?token=TOKEN -H "Content-Type: application/json" -d "{\"total\":10}"

# Проверка статуса
curl http://localhost:8080/status?token=TOKEN

# Получение индекса
curl http://localhost:8080/index

# Получение фото по номеру счетчика
curl http://localhost:8080/index?counterNumber=0011067128144508
```

---

## Чек-лист тестирования

- [ ] Сервер запускается без ошибок
- [ ] API `/start` возвращает токен
- [ ] Браузерный скрипт показывает кнопку синхронизации
- [ ] QR-код генерируется и отображается
- [ ] Опрос статуса работает
- [ ] Android приложение может подключиться
- [ ] Фото находятся и отправляются
- [ ] Фото сохраняются в правильную папку
- [ ] Индекс создается и обновляется
- [ ] Дубликаты правильно определяются
- [ ] Автозагрузка фото работает с синхронизированными фото

---

**Успешного тестирования!** 🚀

